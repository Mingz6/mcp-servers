{
  "name": "mcp-imap-mail",
  "version": "1.0.0",
  "description": "Generic IMAP MCP server — one codebase, deploy once per account (Gmail, QQ Mail, etc.) via env vars",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "imapflow": "^1.0.171",
    "mailparser": "^3.7.2",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/mailparser": "^3.4.5",
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
