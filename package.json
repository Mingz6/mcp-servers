{
  "name": "mcp-servers",
  "version": "1.0.0",
  "private": true,
  "description": "Personal MCP servers monorepo",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "build:teams-chat": "npm run build -w packages/teams-chat",
    "build:outlook": "npm run build -w packages/outlook"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
